language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t magonye/docker-react -f Dockerfile.dev .
  
script:
  - docker run -e CI=true magonye/docker-react npm run test